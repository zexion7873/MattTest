pipeline:
  node-build:
    image: node:latest
    commands:
      - npm ci
      - npm run build
      - cp -R -f dist /usr/share/nginx/html

  show-echo:
    image: alpine
    commands:
      - echo "build success"
      - echo "good job"
      - echo "nice"