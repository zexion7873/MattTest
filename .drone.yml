kind: pipeline
type: docker
name: Drone-Test

platform:
  os: linux
  arch: amd64

steps:
- name: node-build
  image: node:latest
  environment:
    NODE_ENV: gh-pages
  commands:
    - npm ci
    - npm run build

- name: publish
  image: appleboy/gh-pages
  pull: always
  settings:
    username:
      from_secret: sarmiley
    password:
      from_secret: Sarmiley1989
    pages_directory: dist
    target_directory: 2020